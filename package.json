{
  "name": "full-stack-app",
  "version": "1.0.0",
  "scripts": {
      "build": "concurrently \"npm run build:frontend\" \"npm run build:backend\"",
      "build:frontend": "cd front-end && npm start",
      "build:backend": "cd back-end && npm start"
  },
  "dependencies": {
      "axios": "^1.6.5",
      "concurrently": "^6.0.2",
      "react-scripts": "^5.0.1",
      "dotenv": "^16.3.2"
  }
}
