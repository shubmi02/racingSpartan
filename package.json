{
  "name": "full-stack-app",
  "version": "1.0.0",
  "scripts": {
    "build": "concurrently \"npm run build:frontend\" \"npm run build:backend\"",
    "build:frontend": "cd front-end && npm update && npm start",
    "build:backend": "cd back-end && npm update && npm start"
  },
  "dependencies": {
    "axios": "^1.6.5",
    "concurrently": "^6.0.2",
    "dotenv": "^16.3.2",
    "react-scripts": "^3.0.1"
  }
}
